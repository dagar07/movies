import styled from 'styled-components';
import { DisplayRCC } from '../../styles/commonStyle';

export const NoContentStyle = styled(DisplayRCC)`
  color: ${props => props.theme.white};
  font-size: 16px;
  font-weight: 700;
`
